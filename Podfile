# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'roblox_limited_items' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for roblox_limited_items
  pod 'SwiftyJSON'
  pod 'Alamofire'
  pod 'Kingfisher'

end

target 'roblox_limited_items_tests' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for roblox_limited_items_test
  pod 'SwiftyJSON'

end

